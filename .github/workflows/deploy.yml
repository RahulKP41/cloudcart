name: CloudCart CI/CD

on:
  push:
    branches:
      - main

env:
  REGISTRY: docker.io

jobs:

  build-and-push:

    runs-on: ubuntu-latest

    steps:

      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Login Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      #################################
      # USER SERVICE
      #################################

      - name: Build User Image
        run: |
          docker build \
          -t ${{ secrets.DOCKER_USERNAME }}/cloudcart-user-service:latest \
          ./user-service

      - name: Push User Image
        run: |
          docker push \
          ${{ secrets.DOCKER_USERNAME }}/cloudcart-user-service:latest

      #################################
      # PRODUCT SERVICE
      #################################

      - name: Build Product Image
        run: |
          docker build \
          -t ${{ secrets.DOCKER_USERNAME }}/cloudcart-product-service:latest \
          ./product-service

      - name: Push Product Image
        run: |
          docker push \
          ${{ secrets.DOCKER_USERNAME }}/cloudcart-product-service:latest

      #################################
      # ORDER SERVICE
      #################################

      - name: Build Order Image
        run: |
          docker build \
          -t ${{ secrets.DOCKER_USERNAME }}/cloudcart-order-service:latest \
          ./order-service

      - name: Push Order Image
        run: |
          docker push \
          ${{ secrets.DOCKER_USERNAME }}/cloudcart-order-service:latest

  deploy:

    needs: build-and-push

    runs-on: ubuntu-latest

    steps:

      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Configure kubectl
        run: |
          mkdir -p ~/.kube
          echo "${{ secrets.KUBE_CONFIG }}" > ~/.kube/config

      - name: Deploy to Kubernetes
        run: |
          kubectl apply -R -f k8s/

      - name: Restart Deployments
        run: |
          kubectl rollout restart deployment user-deployment
          kubectl rollout restart deployment product-deployment
          kubectl rollout restart deployment order-deployment

      - name: Verify Rollout
        run: |
          kubectl rollout status deployment/user-deployment
          kubectl rollout status deployment/product-deployment
          kubectl rollout status deployment/order-deployment